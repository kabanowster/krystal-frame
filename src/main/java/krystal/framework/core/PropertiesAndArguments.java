package krystal.framework.core;

/**
 * Default properties and arguments. Create your own using {@link PropertiesInterface}.
 */
public enum PropertiesAndArguments implements PropertiesInterface {
	logtofile, logfile, logdir, loglvl, provider
}
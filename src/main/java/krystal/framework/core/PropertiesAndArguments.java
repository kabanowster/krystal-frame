package krystal.framework.core;

/**
 * Default properties and arguments. Create your own using {@link PropertyInterface}.
 */
public enum PropertiesAndArguments implements PropertyInterface {
	logtofile, logfile, loglvl, provider
}